[project]
name = "kyopro-tessoku"
version = "0.1.0"
description = ""
requires-python = ">=3.14"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[tool.black]
line-length = 88

[tool.isort]
profile = "black"
skip_gitignore = true
